<UserControl x:Class="zebra_label_editor.Views.SaveConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:zebra_label_editor.Views"
             xmlns:vm="clr-namespace:zebra_label_editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:UserControl.DataContext>
        <vm:SaveConfigViewModel/>
    </d:UserControl.DataContext>

    <Grid Background="White" Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="Step 4: Save Configuration" FontSize="24" FontWeight="Bold" Margin="0,0,0,30"/>

            <TextBlock Text="File Name:" Margin="0,0,0,5" Foreground="Gray"/>
            <TextBox Text="{Binding FileName}" Padding="5" Margin="0,0,0,15"/>

            <TextBlock Text="Format:" Margin="0,0,0,5" Foreground="Gray"/>
            <ComboBox ItemsSource="{Binding Formats}" 
                      SelectedItem="{Binding SelectedFormat}"
                      Padding="5" Margin="0,0,0,15"/>

            <TextBlock Text="Save Location:" Margin="0,0,0,5" Foreground="Gray"/>
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SavePath}" Padding="5" IsReadOnly="True" Background="#F5F5F5"/>
                <Button Grid.Column="1" Content="..." Command="{Binding BrowseCommand}" Width="40" Margin="5,0,0,0"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="&lt; Back" Command="{Binding GoBackCommand}" Width="100" Padding="10,5" Margin="0,0,10,0"/>
                <Button Content="Process &gt;" Command="{Binding NextCommand}" Width="100" Padding="10,5" Background="LightBlue"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
