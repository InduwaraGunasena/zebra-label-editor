<UserControl x:Class="zebra_label_editor.Views.SelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:zebra_label_editor.Views"
             xmlns:vm="clr-namespace:zebra_label_editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <d:UserControl.DataContext>
        <vm:SelectionViewModel/>
    </d:UserControl.DataContext>

    <Grid Background="White" Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">

            <TextBlock Text="Step 3: Choose Output Method" 
                       FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <Button Command="{Binding SelectPrintCommand}" 
                    Height="80" Margin="0,0,0,15"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" BorderBrush="Gray" BorderThickness="1" Background="White" CornerRadius="5">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                                            <TextBlock Text="🖨️" FontSize="30" VerticalAlignment="Center"/>
                                            <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="Print Labels" FontSize="18" FontWeight="SemiBold"/>
                                                <TextBlock Text="Send directly to a Zebra Printer" Foreground="Gray"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsPrintSelected}" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#E6F7FF"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="#1890FF"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <Button Command="{Binding SelectSaveCommand}" 
                    Height="80" Margin="0,0,0,30"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" BorderBrush="Gray" BorderThickness="1" Background="White" CornerRadius="5">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                                            <TextBlock Text="💾" FontSize="30" VerticalAlignment="Center"/>
                                            <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="Save to File" FontSize="18" FontWeight="SemiBold"/>
                                                <TextBlock Text="Export as PDF or Text files" Foreground="Gray"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsSaveSelected}" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#E6F7FF"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="#1890FF"/>
                                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="&lt; Back" Command="{Binding GoBackCommand}" Width="100" Padding="10,5" Margin="0,0,10,0"/>

                <Button Content="Next &gt;" 
                        Command="{Binding NextCommand}" 
                        IsEnabled="{Binding IsNextEnabled}"
                        Width="100" Padding="10,5" Background="LightBlue"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
