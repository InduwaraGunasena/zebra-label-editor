<UserControl x:Class="zebra_label_editor.Views.ProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:zebra_label_editor.Views"
             xmlns:vm="clr-namespace:zebra_label_editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <d:UserControl.DataContext>
        <vm:ProcessingViewModel/>
    </d:UserControl.DataContext>

    <Grid Background="White" Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="500">
            <TextBlock Text="Processing Labels" FontSize="24" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>

            <ProgressBar Value="{Binding ProgressValue}" Maximum="100" Height="30" Margin="0,0,0,10" Foreground="LightBlue"/>

            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" Foreground="Gray" Margin="0,0,0,30"/>

            <Button Content="Finish &amp; Return Home" 
                    Command="{Binding FinishCommand}" 
                    IsEnabled="{Binding IsFinished}"
                    Width="200" Padding="10,5" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
