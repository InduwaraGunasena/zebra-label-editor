<UserControl x:Class="zebra_label_editor.Views.MappingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:zebra_label_editor.Views"
             xmlns:vm="clr-namespace:zebra_label_editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <d:UserControl.DataContext>
        <vm:MappingViewModel/>
    </d:UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Step 2: Map Data" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Load Excel Sheet..." 
                    Command="{Binding LoadExcelCommand}"
                    Padding="15,5" Background="#DDDDDD"/>

            <TextBlock Text="{Binding ExcelFilePath, StringFormat='Loaded: {0}'}" 
                       VerticalAlignment="Center" Margin="10,0,0,0" 
                       Foreground="Gray" FontStyle="Italic"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding MappingRows}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1" BorderBrush="LightGray">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Label Field (Placeholder)" 
                                    Binding="{Binding ZplPlaceholder}" 
                                    Width="*" 
                                    IsReadOnly="True"
                                    FontWeight="SemiBold"/>

                <DataGridTemplateColumn Header="Map to Source" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">

                                <ComboBox ItemsSource="{Binding AvailableSources}" 
                                          SelectedItem="{Binding SelectedSource}"
                                          Width="200"/>

                                <TextBox Text="{Binding ConstantValue, UpdateSourceTrigger=PropertyChanged}"
                                         Visibility="{Binding IsConstantSelected, Converter={StaticResource BoolToVisConverter}}"
                                         Width="150" Margin="10,0,0,0"
                                         ToolTip="Type the constant value here" 
                                         VerticalContentAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="&lt; Back" Command="{Binding GoBackCommand}" Width="100" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Next &gt;" Command="{Binding NextCommand}" Width="100" Padding="10,5" Background="LightBlue"/>
        </StackPanel>

    </Grid>
</UserControl>
