<UserControl x:Class="zebra_label_editor.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:zebra_label_editor.Views"
             xmlns:vm="clr-namespace:zebra_label_editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <d:UserControl.DataContext>
        <vm:ImportViewModel/>
    </d:UserControl.DataContext>

    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">

            <TextBlock Text="Step 1: Import Label Design" 
                       FontSize="24" FontWeight="Bold" Margin="0,0,0,20" 
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Select your Zebra Designer (.prn/.txt) file:" 
                       Margin="0,0,0,5" Foreground="Gray"/>

            <TextBox Text="{Binding FilePath}" 
                     Padding="5" IsReadOnly="True" Margin="0,0,0,10"/>

            <Button Content="Browse..." 
                    Command="{Binding BrowseCommand}"
                    Padding="10,5" HorizontalAlignment="Right" Width="100"/>

            <TextBlock Text="File Preview:" Margin="0,20,0,5" FontWeight="SemiBold"/>

            <Border BorderBrush="LightGray" BorderThickness="1" Height="100">
                <ScrollViewer>
                    <TextBlock Text="{Binding PreviewContent}" 
                               Padding="5" TextWrapping="Wrap" FontFamily="Consolas"/>
                </ScrollViewer>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Next >" Width="100" Padding="10,5" Background="LightBlue"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
